@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<ul>
    @foreach (var cat in Model.Lista)
    {
        <a asp-page="/Category" asp-route-catId="@cat.Id">
            @cat.Name
        </a>
    }



</ul> 
<ul>
   
</ul>
